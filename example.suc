(set calcularIMC (m h){
  (return (div *m (pow *h 2)))
})

(set m (number (input "insira a massa: ")))
(set h (number (input "insira a altura: ")))
(set imc (calcularIMC *m *h))

(print (format "Seu IMC é: %.2f\n" *imc))